# System packages for SLS tablet appliance (Ubuntu/Lubuntu x86_64)
# Refresh offline cache: scripts/10-fetch-offline.sh
#
# This file lists SEED packages only. 10-fetch-offline.sh also downloads
# transitive hard Depends/PreDepends into vendor/debs/ (FETCH_DEPS=1 default)
# so install-appliance.sh can dpkg -i without apt -f / network.
#
# Note: Ubuntu 24.04+ uses t64 transition names (libfreenect0.5t64).
# Older releases may use libfreenect0.5 — fetch script resolves both.

# --- Kinect depth / IR / motor (app freenect path) — required offline ---
freenect
libfreenect-bin
libfreenect0.5t64
libfreenect-dev
libusb-1.0-0
# --- Spectrum / Record mic (app PortAudio) ---
libportaudio2
alsa-utils
# Kinect USB Audio UAC loader (deb is free; MS firmware via fetch → vendor/kinect/, not git)
# Installs offline when deb is in vendor/debs; firmware file optional private drop
kinect-audio-setup
wget
7zip
# DrakeVox / TTS (keep in sync with sls-camera software/linux/packages/apt-packages.txt)
espeak-ng
python3
python3-venv
python3-pip
python3-dev
# Qt / GUI runtime (PySide6 wheels need these often)
libgl1
libxkbcommon0
libegl1
libxcb-cursor0
libdbus-1-3
# Useful field tools
curl
ca-certificates
usbutils
v4l-utils
# Optional host ffmpeg (app also vendors imageio-ffmpeg wheel)
ffmpeg
dosfstools
udisks2
